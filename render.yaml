services:
  - type: web
    name: sih26162-thermal-anomaly-classifier
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && cd web && npm install && npm run build
    startCommand: python start.py
    healthCheckPath: /health
    autoDeploy: true
